# config/packages/prod/monolog.yaml
monolog:
    handlers:
        main:
            type: error_log
            priority: 10
            level: error
            formatter: monolog.formatter.logstash
            enabled: true
