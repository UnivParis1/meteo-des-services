{% extends 'edit.html.twig' %}

{% block body %}
           {{ form_start(form) }}
            <div class="row mt-3">
                <div class="col-3">
                    <h6 class="text-end">Application<span class="text-danger">*</span></h6>
                </div>
                <div class="col-6">
                    {{ form_widget(form.title, {'attr': {'id': 'signaler_app', 'class': 'form-select rounded', 'style': 'width: 250px;'}}) }}
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-3">
                    <h6 class="text-end">Quel est votre problème ?<span class="text-danger">*</span></h6>
                </div>
                <div class="col-6">
                    {{ form_widget(form.message, {'attr': {'class': 'form-control rounded', 'style': 'width: 250px;'}}) }}
                </div>
            </div>
            <div class="row mt-1">
                <div class="col-6 offset-3 offset-lg-0">
                    <div class="d-flex justify-content-end"><br>Les champs marqués d'un astérisque rouge sont obligatoires.</div>
                    <div class="d-flex justify-content-end fs-6"><i>Ces informations seront envoyées par email à l'équipe météo des services.</i></div>
                </div>
            </div>
            <div class="row mt-5">
                <div class="col-3"></div>
                <div class="col-6">
                    <button type="submit" id="save" class="px-3 py-2 rounded-pill border-0 text-center"><b>Envoyer</b></button>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

